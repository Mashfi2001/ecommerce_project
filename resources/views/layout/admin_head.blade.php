<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Add Product - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .account-container { margin-top: 60px; }
        .form-container {
            background: #fff;
            padding: 50px;
            border-radius: 16px;
            box-shadow: 0 0 100px rgba(0,0,0,0.05);
        }
        .form-btn span {
            font-weight: 600;
            color: #333;
            font-size: 1.2rem;
        }
        .form-btn hr {
            margin-top: 5px;
            height: 3px;
            border: none;
            background: #007bff;
        }
        .form-container input { margin-bottom: 15px; }
        .btn-primary { width: 100%; }
        img { max-width: 100%; }
        .admin-header {
            background: #fff;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
            padding: 20px 0;
            margin-bottom: 30px;
        }
        .admin-header .nav-link {
            color: #333;
            font-weight: 500;
            margin-right: 20px;
            transition: color 0.2s;
        }
        .admin-header .nav-link:hover { color: #007bff; }
        .admin-header .btn-danger { margin-left: 20px; }
    </style>
</head>

